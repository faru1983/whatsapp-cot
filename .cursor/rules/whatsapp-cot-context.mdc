---
description: Contexto del proyecto whatsapp-cot — arquitectura, flujos, convenciones y reglas de edición
alwaysApply: true
---

# whatsapp-cot — Contexto del Proyecto

Bot de WhatsApp para **Cocktails on Tap** (Chile). Guía cotizaciones de **Barriles Desechables** (5L) y **Servicio para Eventos** (Dispensador / Muro).

**Stack:** Node.js ESM (≥20), Baileys, SQLite (`better-sqlite3`), Gemini o Nvidia (LLM), `dotenv`.

## Arquitectura

```
index.js → engine.js → statesMap (flows/index.js)
  ↘ flows/router|barriles|eventos /states/*.js   ← un archivo por paso
  ↘ logic/compile-state.js (defineState)
  ↘ logic/interruptions.js + decision-intent + keyword-intent + nlu-intent
  ↘ logic/validators + actions (registro por nombre)
  ↘ logic/eventos-helpers.js + eventos-style-pack.js + order-builder.js + utils.js + media.js
  ↘ logic/cot-api.js + cot-catalog.js + cot-event-quote.js + cot-barriles-sale.js + cot-contact.js + cot-barriles-contact.js + cot-eventos-contact.js
  ↘ llm.js (fallback/NLU/FAQ) + db.js
  ↘ whatsapp-send.js + business-labels.js
  ↘ views/templates.js (solo compartidos) + prompts.js (solo readPrompt)
  ↘ db/datos.json + db/faq.json
```

| Archivo / carpeta | Rol |
|---|---|
| `src/index.js` | WhatsApp: QR, reconexión, filtros, comandos admin |
| `src/core/engine.js` | Máquina de estados + red de seguridad + CLI (`test:local`) |
| `src/core/llm.js` | `generateResponse`, extractores NLU, `classifyStepIntent`, `responderFAQ` |
| `src/core/db.js` | Sesiones SQLite |
| `src/flows/index.js` | `statesMap` unificado (18 estados) |
| `src/flows/router/states/` | `ESPERANDO_INTENCION` |
| `src/flows/barriles/states/` | Un archivo por `BARRILES_*` |
| `src/flows/eventos/states/` | Un archivo por `EVENTOS_*` |
| `src/flows/cerrado.js` | Estado terminal `CERRADO` |
| `src/logic/compile-state.js` | `defineState` / `compileState` → objeto que espera el engine |
| `src/logic/interruptions.js` | Ruido, precio/carta, mirón, `seguimos` |
| `src/logic/validators/` + `actions/` | Registro de utilidades/acciones por nombre |
| `src/logic/eventos-helpers.js` | Helpers del flujo eventos (formato, carrito, quote) |
| `src/logic/cot-api.js` | Cliente HTTP: catalog, quotes, direct-sales |
| `src/logic/cot-catalog.js` | Catálogo vivo + mapeo carrito → productId/size |
| `src/logic/cot-event-quote.js` | Payload + submit cotización evento (`/quotes`) |
| `src/logic/cot-barriles-sale.js` | Payload + submit venta desechable (`/direct-sales`) |
| `src/logic/cot-contact.js` | Parsers compartidos nombre/apellido/email |
| `src/logic/cot-barriles-contact.js` | Helpers contacto → confirmación → API (Barriles) |
| `src/logic/cot-eventos-contact.js` | Helpers contacto → confirmación → API (Eventos) |
| `src/logic/keyword-intent.js` | Cajita 1: keywords → etiqueta |
| `src/logic/nlu-intent.js` | Cajita 2: clasificador IA |
| `src/logic/decision-intent.js` | Keywords → NLU en menús / sí-no |
| `src/logic/order-builder.js` | Cotización programática |
| `src/logic/media.js` | `img('archivo.ext')` / `vid('archivo.mp4')` desde `assets/` |
| `src/views/templates.js` | Solo textos **compartidos** (cotización, admin, pitches eventos, dudas) |
| `src/views/prompts.js` | Solo `readPrompt()` global (prompts por paso van en cada estado) |

## Un archivo por estado (regla clave)

Cada paso vive en **un solo archivo** bajo `flows/*/states/NOMBRE.js`:

- Textos del paso (`texts` / `promptQuestion` / `shortQuestion`)
- Prompt IA del paso (`aiPrompt` → el engine lo ve como `aiContextPrompt`)
- Lógica (`validateAndProcess`) o, si es menú simple, `type: 'menu'` + `validators[]`

Se define con `defineState({ ... })` desde `compile-state.js`.

```js
// Ejemplo mínimo
export const BARRILES_FILTRO_CANAL = defineState({
  id: 'BARRILES_FILTRO_CANAL',
  texts: [...],          // o promptQuestion
  shortQuestion: '...',
  aiPrompt: `[SISTEMA - ...]`,
  async validateAndProcess(messageText, session) { ... }
});
```

Barrels: `flows/barriles/index.js`, `flows/eventos/index.js`, `flows/router/index.js` → `flows/index.js` arma `statesMap`.

**No** repartir un paso entre `templates.js` + `prompts.js` + monolito. Para cambiar un paso, abrir **solo** su archivo de estado.

## Patrón de estados (contrato engine)

Cada estado exporta (vía `defineState`):
- `id`, `promptQuestion`, `shortQuestion`, `aiContextPrompt`
- `validateAndProcess(messageText, session)` → `{ success, nextState?, customReply?, customReplies?, mute?, notifyAdmin?, shouldReset? }`

`customReplies` (array) = varias burbujas; puede incluir `img(...)` o `vid(...)`.
`customReply` = un mensaje.

**Decisiones de menú / sí-no:** `resolveDecisionIntent` = keywords → NLU. Si fallan → `success: false` → engine: FAQ → LLM corto / plantilla → SOS. **No** usar ese patrón en pasos de datos (fecha, comuna, cócteles, p/p).

**Datos libres:** parser de variantes (número, palabras, frase) → extractor NLU del paso (JSON / número, no prosa) → re-pregunta. Detalle: `conversational-rails.mdc`.

**Interrupciones (prioridad en filtros):** mirón/`después` → mute; precio/lista → dato o carta **sin** fingir CHAT; “qué incluye el servicio” → FAQ / copy de incluido (nunca NLU de productos); saludo/ruido → re-pregunta sin strike; off-topic raro → SOS.

**Estado `CERRADO`:** `customReply` + `mute: true` al entrar; refuerza mute si llega otro mensaje.

## Flujos

**Router:** `ESPERANDO_INTENCION` → BARRILES / EVENTOS / ambas / hint de precios.

**Barriles:** `BARRILES_FILTRO_CANAL` (web + comuna/fecha) → `BARRILES_RECOGIDA_PRODUCTOS` → `BARRILES_REVISION_COTIZACION` (si ya hay datos; si no → `BARRILES_RECOGIDA_DATOS` como red de seguridad) → `BARRILES_ROUTER_MODIFICACION` (modificar) o `BARRILES_DATOS_CONTACTO` (generar compra) → `BARRILES_CONFIRMAR_COMPRA` → `POST /api/v1/direct-sales` → `CERRADO`.

**Eventos:** `EVENTOS_ELECCION_FORMATO` → `EVENTOS_RECOGIDA_DATOS` (tipo → invitados → cócteles p/p + rendimiento) → `EVENTOS_INTRO_MENU` (recomendación + Ver Cócteles y Cotizar / duda→SOS) → catálogo de precios al cotizar → `EVENTOS_ELECCION_MENU` → `EVENTOS_DATOS_CONTACTO` → `EVENTOS_CONFIRMAR_ENVIO` → `POST /api/v1/quotes` → `CERRADO`.

IDs semánticos (`BARRILES_*` / `EVENTOS_*`); transiciones reales en cada `nextState`.

## Red de seguridad (engine.js + index.js)

- **Mute:** intervención humana real, SOS, anti-loop, cierre de venta / mirón.
- **Mute por humano:** solo texto/multimedia real del vendedor; **no** por `protocolMessage`, stubs, reacciones ni ecos del bot.
- **Ephemeral:** desempaquetar con `normalizeMessageContent`.
- **Fallback `success: false`:** ruido → re-pregunta sin IA; FAQ OK (máx. 1 por dato pendiente); sin FAQ → plantilla o LLM corto (2 frases) → 2 strikes SOS.
- **Admin WhatsApp:** `/detenerbot`, `/iniciarbot`, `/reiniciarbot` + número, desde chat admin / “Mensaje para ti mismo”.
- **Labels Business:** engaged → `clientePotencial` (id 4, ensure=false); cierre cotización/venta → `nuevoPedido` (id 6, ensure=false); SOS → `asistencia` (ensure=true).
- **CLI:** `/reset`, `/mute`, `/unmute`, `/exit`.

## Datos y config

- `db/datos.json`, `db/faq.json`
- `.env`: `LLM_PROVIDER`, API keys, `COT_API_BASE_URL`, `COT_API_KEY`, `ADMIN_NUMBERS`, `LABEL_*`, `SECURITY_MAX_CONSECUTIVE_ERRORS`, `SECURITY_MAX_INTENT_SWITCHES`

## Convenciones al editar

Ver también **`coding-standards.mdc`**.

1. Híbrido reglas + IA: validación programática primero; la IA extrae el casillero del paso o responde una ficha FAQ, no cotiza por su cuenta.
2. Minimizar scope: tocar solo el estado/archivo del flujo afectado.
3. Sesión: mutar en `validateAndProcess`; persistir desde `engine.js`.
4. Precios: solo desde `datos.json` / `preciosData`.
5. Negrita WhatsApp: un solo `*asterisco*`.
6. Español chileno cordial; keywords sugeridas en *negrita*.
7. **Nuevo estado:** archivo en `flows/*/states/` + export en barrel del flujo + `npm run verify`.
8. Texto compartido (cotización, admin) → `templates.js`; prompt global → `readPrompt()`.
9. No commitear `.env`, `auth/`, `conversation-memory.sqlite`.
10. Sin legacy: no monolitos `barriles.js` / `eventos.js` / `router.js` ni `STATE_PROMPTS` por estado.
11. Comentarios didácticos (OBJETIVO + JSDoc + pasos importantes).
12. Si el cambio altera copy, assets o burbujas visibles → actualizar `scripts/verify-flows.mjs` en el mismo cambio.

## Quirks conocidos

- Carrito eventos: claves `nombre::litrage` (ej. `Mojito::10L`); barriles: `{ "Mojito": 2 }`.
- Ephemeral: texto dentro de `ephemeralMessage`.
- Documentación: README + esta regla.

## Scripts

- `npm start` — bot WhatsApp real
- `npm run test:local` — simulador CLI
- `npm run check` — syntax check
- `npm run test:flows` — integridad `statesMap` + smoke conversacional
- `npm run verify` — `check` + `test:flows` (usar antes de dar por terminado un cambio de flujos)

## Guía rápida para cambios (ver también `agent-workflow.mdc`, `eventos-implementation.mdc` y `conversational-rails.mdc`)

Cuando el usuario pega una selección de `test:local`, **`[TEST] paso actual: <ID>`** indica el archivo `src/flows/*/states/<ID>.js`. El copy puede estar en helpers (`eventos-intro.js`, `eventos-style-pack.js`, `eventos-helpers.js`). Tras cambiar texto visible → `scripts/verify-flows.mjs` + `npm run verify`. En Eventos: paridad Dispensador/Muro vía helpers de `eventos-helpers.js`.
